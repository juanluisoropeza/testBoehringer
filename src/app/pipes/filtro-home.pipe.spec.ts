import { FiltroHomePipe } from './filtro-home.pipe';

describe('FiltroHomePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHomePipe();
    expect(pipe).toBeTruthy();
  });
});
